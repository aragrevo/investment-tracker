{
  "name": "investment-tracker",
  "private": true,
  "scripts": {
    "local": "dotenv -e .env.local -- pnpm dlx vercel dev"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "bcryptjs": "^3.0.3",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
